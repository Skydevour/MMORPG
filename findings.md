# 土豆 Boss 序列帧分析发现

## 当前资源

- 动作：`idle`、`angry`、`attack_spit`、`hurt`。
- 每个动作 6 帧，共 24 张 PNG。
- 当前所有 PNG 都是 `268×255`。
- 所有帧的不透明像素都落到最底行，垂直落脚线已经一致。

## 偏移根因

- 图片尺寸虽然一致，但角色内容在画布中逐帧向左漂移。
- 以底部 12 像素的不透明像素重心统计，`idle` 从约 `147.43` 漂到约 `112.90`。
- `attack_spit_03` 的右侧动作延伸到 `x=261`，其脚底中心约为 `x=109.29`；直接在原宽度中居中会裁掉右侧动作。
- 当前导入器 Pivot 为 `(0.5, 1/255)`，并非用户要求的严格底部中心。

## 规范化尺寸

- 全局最大水平半径约为 `151.71` 像素。
- 加 4 像素安全边距后，最小对称宽度为 `313`。
- 全局最大内容高度为 `249` 像素，加 4 像素顶部安全边距后，高度为 `253`。
- 目标公共画布：`313×253`，脚底中心像素坐标为 `(156, 252)`。


## 实际处理结果

- 24 张 PNG 已全部离线重写为 313×253。
- 所有帧脚底均贴齐 y=252。
- 所有帧底部 12 像素区域的重心已对齐到 x=156，最大误差为 .490 像素。
- 原文件名、动作目录和 .meta 文件均保留。

## 2026-07-31 新发现

- 用户截图显示 `attack_spit_01` 的主土豆下方存在星形碎片和下一帧角色残片，说明攻击帧在原始裁切阶段已混入非本帧像素。
- 上一版规范化工具以整张图片的所有不透明像素计算脚底中心，因此会把这些误裁切碎片算入锚点，导致 `attack_spit` 的底部中心不正确。
- 修复必须发生在离线资源预处理阶段：先移除不属于主角色连通区域的碎片，再计算公共画布和脚底锚点。
- Hero 冲刺烟雾将改为一次性粒子 Burst，不再加载 `Assets/Res/VFX/DodgeSmoke` 序列帧。

## 2026-07-31 修复结果

- `attack_spit` 的 6 张帧图累计检测到 13 个与主角色间隔超过 24 像素的下方连通区域，确认它们是误裁切的下一帧残片。
- 新增 `RemovePotatoBossFrameArtifacts.ps1`，按最大主角色连通区域的底线过滤下方误裁切碎片；最终验证 24 张帧图中残片数量为 0。
- 清理后重新规范化所有 Boss 帧，公共画布为 `305×192`，脚底中心锚点为 `(152,191)`，最大误差为 `0.466` 像素。
- Hero 闪避已从序列帧播放切换为运行时 `ParticleSystem` Burst：18 个软边粒子，起点与终点各触发一次，Shift 和 L 均生效。
- `Assets/Res/VFX/DodgeSmoke` 的旧序列帧、预览图和元数据已删除，项目内已无该路径引用。
## 2026-08-02 战斗流程验证发现

- Unity 6000 会根据程序集 EditorOnly 标志划分测试平台；PlayMode 测试程序集不能同时依赖 UnityEditor.TestRunner 并声明 EditorOnly。已将测试入口放入 MMORPG.Editor，测试程序集改为 PlayMode 可发现边界。
- 运行时 AddComponent<ParticleSystem> 后默认可能立即播放，必须在设置 duration 前调用 StopEmittingAndClear，否则 Unity Test Framework 会把该警告视为测试失败。
- Boss 攻击、紫色子弹采集、能量清空和大招伤害均已通过真实 MainScene PlayMode 流程确认。
- 默认的测试结果结构由 Unity 写入用户目录，已复制归档到 Plan/playmode_results_2026-08-02.xml。
## 2026-08-03 Boss 连射实现发现

- Boss Sprite 放大后宽度较大，不能简单把出生点设为画面最大 X；Spawner 必须读取 Sprite 世界边界并按左右边界收拢。
- 子弹若使用目标位置归一化向量，会产生斜线飞行；本次改为只取目标的水平相对方向，垂直速度固定为 0。
- 三发连射应由 Boss 控制器计时，状态机只负责 attack_spit 动画状态，这样不会把攻击节奏和序列帧播放器耦合在一起。
- 当前自动化回归的唯一阻塞是外部 Unity Editor 项目锁，不是编译错误。
