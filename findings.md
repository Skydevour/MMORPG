# 土豆 Boss 序列帧分析发现

## PM-008 体验审核

- GameManager.ConfigureCamera 将镜头与地图 LevelHeight 绑定；完整图框 3.2/9=35.6% 占屏，不等于 alpha 轮廓占屏。Boss 放大要处理 Sprite.bounds 的透明留白和站位约束。
- Hero 有中心 VisualPivot；不连贯主要来自空中整段 0.42 秒播完、旋转途中镜像、反馈后重入 jump 与硬回正。详细证据见 PRODUCT_REVIEW_PM-008.md。
- 当前有效 jump 9、run 10、idle 10、dash 8、shoot 7；parry/hurt 借用单帧，不能宣称正式姿态资源完成。
- 近景试调视野高 7.6、中心 (0.2,-0.6)、土豆可见高 4.2；需要重新验证八列泪雨净安全宽度，固定两列可能不再够用。
- UI 改为彩印花园剧场统一主题；PinkCollected 同含射击采集和跳跃反制，不能直接标成反制次数。

## DEV-008 新模式验证发现

- 新输入系统菜单实际按键测试已运行；旧模块已替换并限制为键盘/鼠标，不提前接入手柄。
- 新模式连续重试发现 GardenHazard 静态活动集合保留旧场景引用；在会话启动统一清理全部攻击注册，覆盖新增危险物与玩家大招。
- 音量设置现已作用于活跃声源，受伤时短暂压低枪声；镜头设置显示当前保存值，避免鼠标点击时依赖错误的键盘焦点对象。

## 2026-09-14 DEV-007 实施发现

- 伤害是否接受与弹体是否消耗必须分开；持续危险区用 Stay 补无敌结束后的重新检查。
- 洋葱相邻组安全区若随玩家变化，前组落弹可能覆盖新安全区；已改整轮安全走廊并等待落弹收尾。
- 大招新增预备时序后，旧“按下瞬间扣血”断言失效，已改为预备不伤害、释放后命中。
- 离屏验证中主相机禁用会使 Camera.main 为空；能量 UI 飞入改用 Canvas.worldCamera，并增加回归。
- 新 Boss 正式美术没有可用内置生成工具，采用明确开发占位资源验证；不作为最终视觉验收。
- 一次 apply_patch 因同文件重复操作块被拒绝，已合并重试；首次编译缺 Combat using 已修正。

## 2026-09-13 PM-007 产品审核

- 玩家无敌时 TakeDamage 拒绝伤害，但 BossProjectile 接触后仍无条件回收；同类危险物入口需一起审核。必须分离伤害结果和对象寿命。
- 粉色反制缺少反弹、明确动作和声音；共享烟雾材质无法区分全部命中事件。
- 当前仍为单土豆多阶段，无洋葱/胡萝卜；HUD 直接监听土豆死亡结算，不支持连续遭遇。
- 未发现完整音频链路；本轮用户已授权音频纳入 V0.2。
- 官方网页读取未成功，竞品描述仅作设计经验参考，不作为实时调研证据。

## 2026-09-13 DEV-006 验证发现

- 真实键盘测试发现默认物理摩擦把设置的 5.5 水平速度降到约 5.25，零摩擦材质后匀速断言通过。
- 粒子 World 模拟空间下显式 EmitParams.position 必须使用传入世界位置；修正并增加出生位置测试。
- 批处理缺少 GameView 焦点会过滤模拟键盘；测试临时设置输入焦点策略并恢复，正常玩法不修改输入偏好。
- 屏幕抓图在后台播放器可能为黑色，改用专用验证入口显式离屏绘制；正常玩家入口不受影响。
- 新版证据：14/14 PlayMode、Windows 构建、三尺寸发布截图；详见 DEV-006 交接。PM-006 的历史风险需结合本次修复状态阅读。

## 2026-09-13 产品审核新增发现

- 主角实际 83 帧、Boss 24 帧，分别统一为 307×167 和 305×192；加背景共 108 个目录图片引用，全部可解析。
- run/idle 补间抽查存在双轮廓，尺寸一致不能保证动画质量；没有逐帧确认全部补间。
- 移动按下已立即到目标速度，松手约 0.1 秒减速。射击覆盖跑跳、缓冲提前清除和坐标失配是优先排查项，不能继续把全部问题归因于加速或低帧率。
- 默认白色乘色不构成闪白；继续按钮不恢复 timeScale；连续震屏重取偏移基准；大招直接扣目标血与效果范围脱节。
- Boss 完整图框放大后约宽 11.44，边界夹取使中心最大约 1.83，和 spawnX=5.35 的右侧构图意图冲突。
- 既有 7/7 测试属于旧版本；当前一项测试先锁定 Boss 再期待扣血，需修前置后重新运行。
- 完整证据、推断边界和方案见 `Plan/PRODUCT_VERSION_AUDIT_2026-09-13.md`。

## 当前资源

- 动作：`idle`、`angry`、`attack_spit`、`hurt`。
- 每个动作 6 帧，共 24 张 PNG。
- 当前所有 PNG 都是 `268×255`。
- 所有帧的不透明像素都落到最底行，垂直落脚线已经一致。

## 偏移根因

- 图片尺寸虽然一致，但角色内容在画布中逐帧向左漂移。
- 以底部 12 像素的不透明像素重心统计，`idle` 从约 `147.43` 漂到约 `112.90`。
- `attack_spit_03` 的右侧动作延伸到 `x=261`，其脚底中心约为 `x=109.29`；直接在原宽度中居中会裁掉右侧动作。
- 当前导入器 Pivot 为 `(0.5, 1/255)`，并非用户要求的严格底部中心。

## 规范化尺寸

- 全局最大水平半径约为 `151.71` 像素。
- 加 4 像素安全边距后，最小对称宽度为 `313`。
- 全局最大内容高度为 `249` 像素，加 4 像素顶部安全边距后，高度为 `253`。
- 目标公共画布：`313×253`，脚底中心像素坐标为 `(156, 252)`。


## 实际处理结果

- 24 张 PNG 已全部离线重写为 313×253。
- 所有帧脚底均贴齐 y=252。
- 所有帧底部 12 像素区域的重心已对齐到 x=156，最大误差为 .490 像素。
- 原文件名、动作目录和 .meta 文件均保留。

## 2026-07-31 新发现

- 用户截图显示 `attack_spit_01` 的主土豆下方存在星形碎片和下一帧角色残片，说明攻击帧在原始裁切阶段已混入非本帧像素。
- 上一版规范化工具以整张图片的所有不透明像素计算脚底中心，因此会把这些误裁切碎片算入锚点，导致 `attack_spit` 的底部中心不正确。
- 修复必须发生在离线资源预处理阶段：先移除不属于主角色连通区域的碎片，再计算公共画布和脚底锚点。
- Hero 冲刺烟雾将改为一次性粒子 Burst，不再加载 `Assets/Res/VFX/DodgeSmoke` 序列帧。

## 2026-07-31 修复结果

- `attack_spit` 的 6 张帧图累计检测到 13 个与主角色间隔超过 24 像素的下方连通区域，确认它们是误裁切的下一帧残片。
- 新增 `RemovePotatoBossFrameArtifacts.ps1`，按最大主角色连通区域的底线过滤下方误裁切碎片；最终验证 24 张帧图中残片数量为 0。
- 清理后重新规范化所有 Boss 帧，公共画布为 `305×192`，脚底中心锚点为 `(152,191)`，最大误差为 `0.466` 像素。
- Hero 闪避已从序列帧播放切换为运行时 `ParticleSystem` Burst：18 个软边粒子，起点与终点各触发一次，Shift 和 L 均生效。
- `Assets/Res/VFX/DodgeSmoke` 的旧序列帧、预览图和元数据已删除，项目内已无该路径引用。
## 2026-08-02 战斗流程验证发现

- Unity 6000 会根据程序集 EditorOnly 标志划分测试平台；PlayMode 测试程序集不能同时依赖 UnityEditor.TestRunner 并声明 EditorOnly。已将测试入口放入 MMORPG.Editor，测试程序集改为 PlayMode 可发现边界。
- 运行时 AddComponent<ParticleSystem> 后默认可能立即播放，必须在设置 duration 前调用 StopEmittingAndClear，否则 Unity Test Framework 会把该警告视为测试失败。
- Boss 攻击、紫色子弹采集、能量清空和大招伤害均已通过真实 MainScene PlayMode 流程确认。
- 默认的测试结果结构由 Unity 写入用户目录，已复制归档到 Plan/playmode_results_2026-08-02.xml。
## 2026-08-03 Boss 连射实现发现

- Boss Sprite 放大后宽度较大，不能简单把出生点设为画面最大 X；Spawner 必须读取 Sprite 世界边界并按左右边界收拢。
- 子弹若使用目标位置归一化向量，会产生斜线飞行；本次改为只取目标的水平相对方向，垂直速度固定为 0。
- 三发连射应由 Boss 控制器计时，状态机只负责 attack_spit 动画状态，这样不会把攻击节奏和序列帧播放器耦合在一起。
- 当前自动化回归的唯一阻塞是外部 Unity Editor 项目锁，不是编译错误。

## 2026-08-19 三阶段实现发现

- GameConfig.cs 曾存在未完成的阶段配置改动：引用了未定义的 NormalizePhase 且多一个闭合大括号导致命名空间提前关闭；版本管理前必须先跑 Unity 批量编译确认可编译。
- 新危险物必须纳入"阶段切换清场"：除 BossProjectile 外，BossRootHazard/BossInsect/BossLobbedSeed 都要维护活动集合并提供 DespawnAllActive，否则转阶段后场上残留危险物。
- 旧 PlayMode 断言"玩家死亡后立即显示失败界面"不成立：失败结算按配置延迟 2 秒显示，断言必须等待 deathResultDelay 之后再检查。
- 新增危险物会显著提高站桩测试玩家的压力：抛物线种子落地后是持续伤害障碍，测试中应通过无敌帧或位移避免测试玩家在能量流程断言前死亡。
- 阶段 1 抛物线种子按规格应"交替出现"，不能每轮随机独立判定，否则可能连续多次种子攻击；用上次攻击标记强制交替。

## 2026-08-21 操作与战斗闭环检查

### 已确认

- 受击逻辑先检查 `IsInvincible`，将配置的 3 秒窗口覆盖普通受击；冲刺、大招和死亡仍通过同一无敌入口保护。
- 粉色子弹现在由 `PlayerController2D.TryBreakPinkProjectile` 统一判断空中状态，再由投射物执行能量结算，避免地面接触也能刷能量。
- FrameAnimator 负责换帧时的可视中心对齐，PlayerSpawner 只负责创建物理根节点和视觉中心节点，角色翻滚不再依赖某一帧的 Sprite 角点。
- HUD 根节点补充 `RectTransform`，Boss 血条的顶部中央锚点在 Canvas 尺寸变化时保持稳定。

### 当前阻塞

- 当前环境没有可直接调用的 Unity Editor PlayMode 会话，因此无法代替人工确认实际画面、物理触发和按钮鼠标点击。
- 导出的 `.csproj` 不是 Unity 的唯一事实来源；本轮为本地构建临时补入新增 UI 脚本，Unity 重导出后需要复核。

## 2026-08-20 框架加固结果

### 已解决

- Boss 阶段切换期间仍可被连续子弹扣血：改为显式 `currentPhase`，安全窗内直接忽略伤害，阶段只触发一次。
- JSON 显式空对象导致 `NormalizePhase` 空引用：增加所有配置段和三阶段配置段的默认实例保护，并将规范化纳入加载异常兜底。
- 玩家失败后危险物仍在场运行：`GameManager` 在失败结算前清理四类 Boss 危险物。
- 对象池清场缺少回归证据：新增四类危险物的活动登记和 `DespawnAllActive()` 清场测试。

### 当前残余风险

- 阶段 3 危险物仍使用运行时占位贴图，需后续由策划和美术接入正式资源。
- 自动化测试已覆盖状态和生命周期，但三阶段的实际操作手感、视觉可读性和难度仍需人工 Play Mode 试玩。
- `GameConfigService` 使用静态缓存，当前由 `GameManager.Awake()` 显式重载；后续若增加非场景入口，需要再设计配置会话生命周期。
# DEV-008 补充审核（2026-09-14）

- 最终完整测试 36/36（192.589 秒），但退出清理停滞后结束本任务 PID 69344，退出码 -1；最终构建及三尺寸播放器均正常退出 0。当前实际键盘自动驾驶结果 56.6 秒/3 HP/9 粉弹/3 次大招，完整三形态 Victory。

- 新模式输入与十次连续重试 13/13 通过；该批 Unity 写完成功 XML 后退出清理停滞，仅结束了确认属于本任务的 PID 101464，退出码 -1，不能记为正常退出。
- 新增激光锁定、早/准/晚闪避、追踪弹转向/寿命、洋葱安全走廊测试后 18/18 通过，Unity 正常退出码 0。
- 产品/策划只读复核发现：土豆背后安全输出、泪雨列间永久缝隙、设置焦点逃逸、过渡跳跃缓存、暂停缺少重开/标题入口。
- 产品已确认窄改：玩家碰撞体右缘限制在 Boss 最终尺寸左缘前 0.1，阶段切换复用；洋葱按真实可达范围分列并冻结变化落点，净安全宽度至少 1.6；设置内部导航和暂停入口完善。不得把代码方案确认当成体验验收。
- 已接入原创离线合成 41 个 WAV，音乐为 16 小节立体声编曲，零削波仅是文件级检测，不代表完整听感通过。
- 当前正式美术阻塞未解除：没有可调用 image_gen，未配置图像 CLI 所需 API 密钥。洋葱/胡萝卜仍为明确标记的开发占位，不允许作为正式发布原画。
