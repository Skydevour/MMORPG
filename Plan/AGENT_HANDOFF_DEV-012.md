# DEV-012 入场播放接入交接

2026-09-14，开发米浴；依据ART-012及用户确认。代码阶段交付，正式美术未完成；未提交推送，未更新测试包。

## 修改

- BossEntrancePlayer/BossEntranceClip：每位Boss可绑定完整入场序列及帧率，独立视觉层播放，根节点/碰撞体/站位不缩放；播放完成、取消和禁用分别处理，恢复身体显示，暂停不推进时间。
- GardenBossAssets：新增potatoEntrance/onionEntrance/carrotEntrance三个序列槽位，可在现有Catalog绑定各自动作。当前无正式帧，三个槽位不伪造内容。
- EncounterDirector：删除统一纵向缩放及固定0.6秒入场比例；退场等待使用exitDuration配置，换形态后等待该角色入场播放器完成再开战。无帧时保留已有待机等待并发出中文资源缺口警告，不宣称实现破土演出。
- 洋葱/胡萝卜攻击使用完整数组分成预备/释放/恢复，取消固定只取前六帧；至少三张真实帧。当前三等分是兼容接入方式，正式动作阶段划分仍须按原画标定，不能以数量代替动作语义。
- BattleTitleView：标题销毁恢复角色时尊重入场播放器的显示控制，避免身体与入场帧叠加。

## 验证

- Unity资源准备/编译及原图保护校验通过，unity_DEV-012_prepare.log返回0。
- 首轮完整PlayMode57/57，215.18秒，playmode_results_DEV-012.xml。XML完成后Unity退出清理停滞；等待后仅终止本项目PID116040，不把它称为正常退出。
- 只读交叉审查发现标题恢复覆盖、取消误算完成、无帧等待描述不准确，已修复。
- 修复后入场/标题三形态/过渡专项4/4，8.62秒，playmode_results_DEV-012_final.xml，日志确认测试退出0。未再次跑全量57项。
- 测试使用临时合成Sprite验证生命周期与变换不变；这不是正式Boss原画，也不进入运行资源。

## 尚未完成

UI重绘、洋葱/胡萝卜新造型、土豆鼓土/破土/落稳、三Boss真实待机微动作、分层入场效果和声音仍缺正式资源。当前运行只删除了错误的统一缩放，缺帧时仍是旧待机等待，不能称为已达到用户参考图。

用户已经授权CLI图像生成；本机Process/User/Machine均无OPENAI_API_KEY且无内置生成工具，尚未生成任何新原画。参考图、提示词与动作要求均已保存于Plan/Art，配置好本机密钥后继续生产和实机验收。现有DEV-011包不包含本轮源码改动，没有把它标成新包。
