# DEV-011 土豆时间轴交接

## 基本信息

- 开发专家，2026-09-14；状态：待验收，修改锁已释放，主会话可接手。
- 依据：用户明确授权、PM-010、DEV-010 实施规格。本次仅土豆统一时钟，不涉及 Player、UI、定向受击反馈或 GameConfig 主文件。

## 交付内容

- Framework/Animation/OneShotTimeline.cs：非循环一次性标记，事件编号为有序标记索引；跨帧包含右端点，每次推进最多消费一个，停在标记时间并舍弃剩余时间，不积压瞬时补发。零增量暂停；取消不可恢复，重新攻击创建新实例。严格拒绝重复/乱序/越界标记和非法增量。
- Bosses/Potato/PotatoAttackSequence.cs：预备、释放、恢复共享同一时钟。六帧显示单调向前，第3至4帧释放，最后进入恢复。三发间隔和三路高度仍读取原配置。
- PotatoBossController.cs：移除 attackShotTimer、attackTimer、attackElapsed 及循环补发；每个 release 同步生成弹体/播放吐气音效。死亡、战斗锁、阶段切换、禁用取消旧序列；暂停/关闭 AI 冻结推进。每轮最多一次中文严重跨帧诊断。
- PotatoBossStateDriver.cs：执行顺序50，在控制器推进后、动画器100之前采样，避免显示落后一帧。没有修改通用 FrameAnimator 的既有改动。
- Assets/Tests/PlayMode/PotatoAttackTimelineTests.cs、Assets/Tests/RunPotatoTimeline.ps1 和新增文件 meta。

## 验证证据

- 零时长返修：预备/恢复支持0，与 EncounterConfig.Validate 对齐；间隔仍为有限正数。Framework支持零总时长，零增量不触发零时刻标记，首个正增量恰好消费一次。零恢复在最后释放后立即逻辑完成；控制器额外保留当帧释放显示，下一次有效更新退出攻击，不添加虚构恢复时长。
- 返修后独立纯逻辑测试 **11/11通过**，新增零预备、零恢复、两者为零的三发及单发测试。控制器当帧显示保持仍待主会话Unity集成回归。第3至4帧释放区不代表逐发完整嘴型达标。

- `pwsh -NoProfile -File Assets/Tests/RunPotatoTimeline.ps1`：7/7 通过，直接编译运行同一 NUnit 测试源码。覆盖零增量、边界不重发、释放前取消/新序列、极端跨帧三弹与完整恢复、30/60/120步进单调采样、非法参数。
- 首次独立运行缺少 mscorlib 引用，脚本补充 PowerShell ref/mscorlib.dll 后通过。
- 未启动 Unity，未执行 Runtime/Editor/PlayMode 整体编译、实际弹体计数、实机帧采样或音频起音测试；上述纯逻辑证据不能替代集成验收。新源码需要主会话 Unity 刷新后纳入生成 csproj。
- 未提交、未推送；保留工作区所有既有改动。

## 主会话回归步骤

1. 接回锁完成 Player/定向 Hit 反馈后，刷新并编译 Runtime、Editor、测试程序集，运行 PotatoAttackTimelineTests 与原战斗回归。
2. 三形态战斗中记录土豆每轮3发，逐发检查动画器索引2或3及同帧生成/音效调用；渲染器不可见时也应照常发弹。
3. 在首发前、发间及恢复中暂停/继续；死亡、战斗锁、禁用、重试后确认无旧轮事件。测试30/60/120和人为卡帧。
4. 卡帧每次至多一发，随后仍保留配置间隔，第三发后完整恢复才进入冷却。时间舍弃意味着正常帧也可能有不足一帧的每发延长，这是保留可见释放帧的明确取舍。

## 风险与资源缺口

- 源图烘焙飞土，第3至4帧嘴部展开，但没有三路独立嘴型及逐帧嘴位。当前保持原三路高度和原图，不伪造嘴型、不改坐标掩盖差异；三发在现有展开区间释放，不宣称每发都有独立开合演出。
- 时钟修复不等于资源同步完全验收。正式三路嘴型、源图去除烘焙飞土、口部效果/实际发弹位置精确重合、声音起音和完整恢复视觉仍需正式美术与实机复核。
- 通用能力本轮明确为非循环；未新增循环播放器标记契约，不把 DEV-010 全部范围标记完成。
